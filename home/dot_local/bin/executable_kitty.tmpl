#!/usr/bin/env bash

args=()

if [[ $(pwd) == "$HOME" ]]; then
    args+=(--directory ~/Desktop)
fi

{{ if (index .features "vm") }}LIBGL_ALWAYS_SOFTWARE=1 LIBGL_ALWAYS_SOFTWARE=true GALLIUM_DRIVER=llvmpipe{{ end }} \
exec nixGLMesa ~/.nix-profile/bin/kitty "${args[@]}" "$@"

